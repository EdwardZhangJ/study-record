
const initialState = {
  userInfo: {
    nickname: 'kobe',
    level: 100
  }
}

function reducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state
  }
}

export default reducer
